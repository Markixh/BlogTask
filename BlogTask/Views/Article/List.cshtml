@model BlogTask.Models.Article.ListViewModel;

@if (Model.List.Count != 0)
{
    @foreach (var user in @Model.List)
    {
        <div class="card">
            <h5 class="card-header">Заголовок статьи</h5>
            <div class="card-body">
                <span class="badge bg-primary">Теги</span>
            </div>
        </div>
    }
}